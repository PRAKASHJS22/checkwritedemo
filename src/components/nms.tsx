import React from "react";
import { colors } from "../constants/colors";

const NmsProduction_Accounting = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        backgroundColor: colors.blue,
        height:"6.28vh",
        alignItems:'center'
      }}
    >
     <h5 style={{color:colors.white,fontWeight:600,fontSize:'1em'}}> NMS PRODUCTION ACCOUTNING</h5>
    </div>
  );
};

export default NmsProduction_Accounting;
